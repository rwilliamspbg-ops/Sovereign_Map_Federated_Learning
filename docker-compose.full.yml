services:
  # ========================================================================
  # SOVEREIGN MAPS BACKEND (Flower Aggregator + Flask Metrics)
  # ========================================================================
  
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sovereign-backend
    environment:
      - PYTHONUNBUFFERED=1
      - FLOWER_SERVER_PORT=8080
      - FLASK_PORT=8000
      - NUM_ROUNDS=100
      - MIN_FIT_CLIENTS=1
      - GEMINI_API_KEY=${GEMINI_API_KEY:-}
    ports:
      - "8000:8000"  # Flask metrics API
      - "8080:8080"  # Flower aggregator
    volumes:
      - ./sovereignmap_production_backend_v2.py:/app/sovereignmap_production_backend_v2.py:ro
      - ./src:/app/src:ro
    networks:
      - sovereign-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 30s
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    labels:
      - "description=Sovereign Maps Backend with Flower aggregator"

  # ========================================================================
  # FEDERATED LEARNING NODES (Flower Clients)
  # ========================================================================
  
  node-agent:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - PYTHONUNBUFFERED=1
      - NODE_ID=${NODE_ID:-1}
      - AGGREGATOR_HOST=backend
      - AGGREGATOR_PORT=8080
      - BYZANTINE=${BYZANTINE:-false}
    command: >
      python -u src/client.py
      --node-id ${NODE_ID:-1}
      --aggregator backend:8080
    volumes:
      - ./src:/app/src:ro
      - ./requirements.txt:/app/requirements.txt:ro
    working_dir: /app
    networks:
      - sovereign-network
    depends_on:
      backend:
        condition: service_healthy
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    labels:
      - "description=Sovereign Maps Federated Learning Node"

  # ========================================================================
  # MONITORING STACK
  # ========================================================================
  
  prometheus:
    image: prom/prometheus:latest
    container_name: sovereign-prometheus
    ports:
      - "9090:9090"
    volumes:
      - prometheus-data:/prometheus
      - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--storage.tsdb.retention.time=30d'
    networks:
      - sovereign-network
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:9090/-/healthy"]
      interval: 10s
      timeout: 5s
      retries: 3
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  grafana:
    image: grafana/grafana:latest
    container_name: sovereign-grafana
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=admin
      - GF_INSTALL_PLUGINS=grafana-piechart-panel
    volumes:
      - grafana-data:/var/lib/grafana
      - ./monitoring/dashboards:/etc/grafana/provisioning/dashboards:ro
    networks:
      - sovereign-network
    depends_on:
      - prometheus
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/api/health"]
      interval: 10s
      timeout: 5s
      retries: 3
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  alertmanager:
    image: prom/alertmanager:latest
    container_name: sovereign-alertmanager
    ports:
      - "9093:9093"
    volumes:
      - alertmanager-data:/alertmanager
      - ./tpm_alerts.yml:/etc/alertmanager/config.yml:ro
    command:
      - '--config.file=/etc/alertmanager/config.yml'
      - '--storage.path=/alertmanager'
    networks:
      - sovereign-network
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

# ========================================================================
# VOLUMES
# ========================================================================

volumes:
  prometheus-data:
    driver: local
  grafana-data:
    driver: local
  alertmanager-data:
    driver: local

# ========================================================================
# NETWORKS
# ========================================================================

networks:
  sovereign-network:
    driver: bridge
    driver_opts:
      com.docker.network.bridge.name: br-sovereign
