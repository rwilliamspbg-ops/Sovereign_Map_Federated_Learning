# Demo GIF Pack

Short demo loops used by the landing page and README.

## Assets

- `sandbox-up.gif` - one-command startup path from `make sandbox-up` to a ready stack.
- `aggregation-round.gif` - a three-node aggregation round that highlights the trust hierarchy.
- `proof-verification.gif` - proof verification and audit confirmation flow.

## Notes

- These GIFs are sized for README and Pages usage.
- They are intentionally short so the landing page stays fast to load.
- If you update the stack visuals, regenerate these assets together with the screenshots.
