# Use a lightweight base image suitable for a monitoring sidecar or exporter
FROM python:3.9-slim

# Set the working directory
WORKDIR /app

# Install basic monitoring/health-check tools if needed
RUN apt-get update && apt-get install -y curl && rm -rf /var/lib/apt/lists/*

# Copy any monitoring scripts if you have them (optional)
# COPY monitoring_script.py .

# Example command to keep the container running or start a prometheus-style exporter
CMD ["python3", "-m", "http.server", "3000"]
