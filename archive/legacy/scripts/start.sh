#!/bin/bash

# Start all components using docker-compose

# Navigate to the directory containing docker-compose.yml


# Start all services
docker-compose up -d

# Manage frontend and backend services
# Add additional commands for managing frontend/backend as needed
